{% include 'default/_form.html.twig' with {
    page_title: 'title_password_change',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('user_password_change', {'id': user.id}),
    },
    back_to_list_path: 'recipe_index',
} %}